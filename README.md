# project-ego

Mateus Munhoz & Leonardo Henz
Info23

Na pasta do Back rode:

npm install express socket.io cors
npm install -D typescript ts-node-dev @types/node @types/express @types/socket.io

npm run dev


Na pasta do Front rode:

npm install

npm start